# Tailscale Operator Helm Values

# Resource requests and limits
resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi

# RBAC settings
rbac:
  create: true

serviceAccount:
  create: true
